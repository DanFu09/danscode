<% provide(:title, "Projects") %>
<div class="page-header">
	<h1>Projects</h1>
</div>
<%= render 'projects/current_projects', :project => @current_projects %>
<%= render 'projects/past_projects', :project => @past_projects %>